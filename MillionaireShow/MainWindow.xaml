<Window x:Class="MillionaireShow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MillionaireShow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="{Binding Question}"/>
        <TextBlock Text="{Binding Answer1}"/>
        <TextBlock Text="{Binding Answer2}"/>
        <TextBlock Text="{Binding Answer3}"/>
        <TextBlock Text="{Binding Answer4}"/>
    </StackPanel>
</Window>
